# 项目协作约定

- 禁止执行站点或应用构建命令（例如 `pnpm build`、`pnpm --filter docs build`），除非用户明确允许；需要语法检查时优先使用 `typecheck` 等非构建命令。
- `pnpm --filter docs registry:build` 始终允许执行，用于同步 registry、文档预览索引、属性元数据和 `public/r/*.json`。
- 不要主动运行 `pnpm dev`，避免与用户正在运行的开发服务冲突。
- 尽量使用 `pnpm`，不要使用 `npm`。
- 除非用户明确要求，不要主动编写测试用例。
- 当前对话处于开发过程，不需要编写兜底逻辑。
- UI 设计避免常见的 AI 模板化倾向：不要滥用大圆角、卡片套卡片、玻璃拟态、白色内高光、重阴影、渐变和无意义装饰；优先建立清晰层级、合理密度与产品语义。
- 组件或 UI 实现完成后，默认无需主动打开浏览器、运行 Playwright 或进行浏览器视觉验收，由用户直接查看效果；仅在用户明确要求时执行。

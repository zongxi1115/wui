{
  "name": "upload",
  "type": "registry:ui",
  "title": "上传 Upload",
  "description": "支持点击选择、拖拽放置与上传状态反馈的文件上传组件。",
  "import": "import { Upload, uploadVariants } from \"@/components/ui/upload\"",
  "exports": [
    "Upload",
    "uploadVariants"
  ],
  "typeExports": [
    "UploadProps",
    "UploadStatus"
  ],
  "install": {
    "wui": "npx wui@latest add @wui/upload",
    "shadcn": "npx shadcn@latest add https://ui.wzx.wang/r/upload.json",
    "npmDependencies": [
      "lucide-react",
      "class-variance-authority",
      "motion"
    ],
    "registryDependencies": [
      "utils"
    ]
  },
  "props": [
    {
      "name": "label",
      "type": "string",
      "required": false,
      "control": "text",
      "defaultValue": "Drop files here or click to browse",
      "description": "Main instruction displayed inside the drop zone."
    },
    {
      "name": "description",
      "type": "string",
      "required": false,
      "control": "text",
      "description": "Supporting copy displayed under the main instruction."
    },
    {
      "name": "status",
      "type": "\"idle\" | \"error\" | \"complete\" | \"uploading\"",
      "required": false,
      "control": "select",
      "options": [
        "idle",
        "error",
        "complete",
        "uploading"
      ],
      "defaultValue": "\"idle\"",
      "description": "Visual lifecycle state."
    },
    {
      "name": "progress",
      "type": "number",
      "required": false,
      "control": "number",
      "defaultValue": "0",
      "description": "Determinate upload progress from 0 to 100."
    },
    {
      "name": "size",
      "type": "\"compact\" | \"default\"",
      "required": false,
      "control": "select",
      "options": [
        "compact",
        "default"
      ],
      "defaultValue": "default",
      "description": "Height and spacing preset."
    },
    {
      "name": "multiple",
      "type": "boolean",
      "required": false,
      "control": "boolean",
      "defaultValue": "false",
      "description": "Allows more than one file to be selected."
    },
    {
      "name": "accept",
      "type": "string",
      "required": false,
      "control": "text",
      "description": "Comma-separated list of accepted MIME types or file extensions."
    },
    {
      "name": "onFilesChange",
      "type": "((files: File[]) => void)",
      "required": false,
      "control": "text",
      "description": "Called whenever files are selected or dropped."
    },
    {
      "name": "onUpload",
      "type": "((files: File[]) => void | Promise<void>)",
      "required": false,
      "control": "text",
      "description": "Called after file selection and reflected in the internal lifecycle state."
    },
    {
      "name": "disabled",
      "type": "boolean",
      "required": false,
      "control": "boolean"
    }
  ],
  "usage": "`Upload` 将文件选择、拖拽区域和上传反馈放在同一个入口中，适合附件、媒体资源和批量文件上传。示例在上传完成后用 `Download` 展示文件，形成从提交到取回的完整文件流。\r\n\r\n组件负责文件选择与交互状态，不预设请求方式。使用 `onFilesChange` 只获取文件，或通过 `onUpload` 返回 Promise，让组件自动展示上传中、完成与失败状态。",
  "extended": "### 尺寸与状态\r\n\r\n\r\n\r\n`status` 支持 `idle`、`uploading`、`complete` 与 `error`；传入 `progress` 可显示 0–100 的确定进度。使用 `accept` 限制系统文件选择器中的类型，使用 `multiple` 开启多选。",
  "events": "- `onFilesChange(files)`：用户选择或拖入文件后立即调用。\r\n- `onUpload(files)`：选择文件后调用；返回 Promise 时，非受控模式会跟随其生命周期更新状态。\r\n- 原生表单属性以及 `onFocus`、`onBlur` 等输入事件会继续透传到隐藏的文件输入框。",
  "examples": [
    {
      "name": "upload-demo",
      "title": "基础示例"
    },
    {
      "name": "upload-states",
      "title": "尺寸与状态"
    }
  ],
  "files": [
    "registry/ui/upload.tsx"
  ],
  "docsUrl": "https://ui.wzx.wang/docs/components/upload",
  "sourceUrl": "https://ui.wzx.wang/r/upload.json"
}

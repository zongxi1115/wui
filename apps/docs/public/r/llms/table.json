{
  "name": "table",
  "type": "registry:ui",
  "title": "表格 Table",
  "description": "用原生表格语义组织和比较结构化数据。",
  "import": "import { Table, TableBody, TableCaption, TableCell, TableFooter, TableHead, TableHeader, TableRow, tableVariants } from \"@/components/ui/table\"",
  "exports": [
    "Table",
    "TableBody",
    "TableCaption",
    "TableCell",
    "TableFooter",
    "TableHead",
    "TableHeader",
    "TableRow",
    "tableVariants"
  ],
  "typeExports": [
    "TableCellProps",
    "TableHeadProps",
    "TableProps"
  ],
  "install": {
    "wui": "npx wui@latest add @wui/table",
    "shadcn": "npx shadcn@latest add https://ui.wzx.wang/r/table.json",
    "npmDependencies": [
      "class-variance-authority"
    ],
    "registryDependencies": [
      "utils"
    ]
  },
  "props": [
    {
      "name": "containerClassName",
      "type": "string",
      "required": false,
      "control": "text",
      "description": "Extra classes applied to the horizontal overflow container."
    },
    {
      "name": "density",
      "type": "\"compact\" | \"default\"",
      "required": false,
      "control": "select",
      "options": [
        "compact",
        "default"
      ],
      "defaultValue": "default",
      "description": "Controls cell padding and row height."
    },
    {
      "name": "striped",
      "type": "boolean",
      "required": false,
      "control": "boolean",
      "defaultValue": "false",
      "description": "Adds a subtle background to alternating body rows."
    },
    {
      "name": "stickyHeader",
      "type": "boolean",
      "required": false,
      "control": "boolean",
      "defaultValue": "false",
      "description": "Keeps the header visible while the table container scrolls."
    }
  ],
  "usage": "`Table` 用于展示具有稳定列结构、需要横向比较的数据。组件保留原生表格元素的语义，适合账单、成员列表、用量明细和配置清单。小屏幕下外层容器会横向滚动，避免压缩内容或破坏列关系。\r\n\r\n如果内容主要用于页面布局、只有单列键值，或需要虚拟滚动和复杂数据处理，应选择更合适的布局或数据网格方案。",
  "extended": "### 紧凑密度与斑马纹\r\n\r\n`density=\"compact\"` 减少单元格留白，适合信息密度较高的后台页面；`striped` 为相邻数据行添加轻微层次。`TableFooter` 可承载合计或汇总信息。\r\n\r\n\r\n\r\n使用 `className` 控制表格本身，使用 `containerClassName` 定制横向滚动容器。数值列建议右对齐并添加 `tabular-nums`，以便快速比较。",
  "events": "所有原生表格事件都会透传，例如在 `TableRow` 上使用 `onClick`、`onPointerEnter` 或键盘事件。组件不内置选择逻辑；需要展示选中状态时，在行上设置 `data-state=\"selected\"`，并由业务状态负责更新。",
  "examples": [
    {
      "name": "table-demo",
      "title": "基础示例"
    },
    {
      "name": "table-compact",
      "title": "紧凑密度与斑马纹"
    },
    {
      "name": "table-advanced",
      "title": "高级数据表格"
    }
  ],
  "files": [
    "registry/ui/table.tsx"
  ],
  "docsUrl": "https://ui.wzx.wang/docs/components/table",
  "sourceUrl": "https://ui.wzx.wang/r/table.json"
}

---
title: 滚动展开 Scroll Expand
description: 随着滚动进度将画框中的媒体内容平滑展开至全屏无缝铺满（或从全屏收缩为卡片）的沉浸式视差组件。
component: scroll-expand
---

## 基础用法

向下滚动，带有圆角与内边距的画框媒体将平滑扩展为铺满视口边缘的全画幅大图：

<ComponentPreview name="scroll-expand-demo" />

## 安装与引入

通过 CLI 自动添加组件，或手动复制源码至项目中：

<CodeTabs command="@wui-design/cli@latest add @wui/scroll-expand" />

<Steps>

<Step>安装基础依赖与动效库</Step>

```bash
pnpm add motion lucide-react class-variance-authority clsx tailwind-merge
```

<Step>复制组件源码到 `components/ui/scroll-expand.tsx`</Step>

<ComponentSource name="scroll-expand" title="components/ui/scroll-expand.tsx" />

</Steps>

## 属性 Props

<PropsTable
  data={[
    {
      prop: "children",
      type: "React.ReactNode",
      description: "在视口中展开或收缩的视频、高分辨率图片或沉浸式 Hero 内容。",
    },
    {
      prop: "direction",
      type: '"expand" | "collapse"',
      default: '"expand"',
      description: "变换方向。`expand` 表示随向下滚动由紧凑卡片展开为满屏；`collapse` 表示由满屏收缩为卡片。",
    },
    {
      prop: "inset",
      type: "number",
      default: "10",
      description: "紧凑初始状态下画框四周的内缩百分比（%）。",
    },
    {
      prop: "radius",
      type: "number",
      default: "28",
      description: "紧凑状态下画框的圆角大小（像素）。在完全展开态下会自动平滑过渡至 0px。",
    },
    {
      prop: "scrollLength",
      type: "number",
      default: "1.8",
      description: "动画占用的总滚动距离（以当前视口高度的倍数计算，例如 1.8 代表 1.8 个视口高度）。",
    },
    {
      prop: "container",
      type: "React.RefObject<HTMLElement | null>",
      description: "局部滚动容器的 ref 引用（不传时监听整个全局页面滚动）。",
    },
    {
      prop: "viewportClassName",
      type: "string",
      description: "应用于吸顶固定视口容器（Sticky Viewport）的 CSS 类名。",
    },
    {
      prop: "frameClassName",
      type: "string",
      description: "应用于由 clip-path 驱动的裁切画框的 CSS 类名。",
    },
    {
      prop: "className",
      type: "string",
      description: "应用于外层占位容器的 CSS 类名。",
    },
  ]}
/>

## 事件 Events

<PropsTable
  data={[
    {
      prop: "onScroll",
      type: "(event: React.UIEvent) => void",
      description: "滚动事件监听函数。",
    },
  ]}
/>

## 使用场景与设计规范

`ScrollExpand` 适用于**大片级产品宣传、全景摄影展示、旗舰设备发布会网页**：

- **从精致卡片过渡到震撼大图**：用户在阅读前置文字时处于克制的信息接收状态，一旦开始滚动，画框向四周炸开铺满，带来强烈的沉浸视觉冲击。
- **高性能 Clip-path 裁切**：使用 CSS `clip-path: inset(...)` 与 GPU 硬件加速缩放，避免修改 DOM 物理宽高带来的重排（Reflow）性能损耗。

## 场景示例

### 逆向收缩为卡片（Collapse）

从全屏沉浸封面随滚动渐进收缩为紧凑的焦点卡片：

<ComponentPreview name="scroll-expand-collapse" />

## 无障碍与交互 Accessibility

- **减少动效环境处理**：当系统启用 `prefers-reduced-motion: reduce` 时，clip-path 将锁定为直接全铺状态，且取消任何缩放变换，保证视力敏感用户顺畅浏览。
- **内容文字可访问**：覆盖在大图之上的标题与文案依然使用标准语义标签（如 `<h1>`、`<p>`），屏幕阅读器能准确提取全部语义。

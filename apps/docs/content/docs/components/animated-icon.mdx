---
title: 动态图标
description: 图标内部路径会随交互运动的 Lucide 动态图标集合。
component: animated-icon
---

## 基础示例

这些不是对整个 SVG 做统一缩放或旋转。每个图标都有独立的逐路径动画，例如铃铛摆动、复制层分离、菜单线条重排和齿轮转动。

<ComponentPreview name="animated-icon-demo" />

安装组件：

<CodeTabs command="wui@latest add @wui/animated-icon" />

安装命令会加入 `@animateicons/react`。图标基于 Motion 实现，默认响应悬停，也支持从父组件通过 ref 控制播放。

<ComponentSource name="animated-icon" title="components/ui/animated-icon.tsx" />

## 动态图标库

当前整合 AnimateIcons 的 Lucide 集合。悬停或键盘聚焦任意图标即可预览动画，点击会复制按需导入语句。

<AnimatedIconLibrary />

## 直接使用

```tsx
import { BellRingIcon, CopyIcon } from "@animateicons/react/lucide"

<BellRingIcon size={24} />
<CopyIcon size={24} duration={0.7} />
```

每个图标默认在自身悬停时播放。如果交互区域比图标大，例如工具栏按钮或菜单项，可以使用 wui 的 `AnimatedIcon` 统一处理无障碍语义，并通过 ref 让父元素启动动画。

## 从父元素控制

```tsx
"use client"

import { useRef } from "react"
import { BellRingIcon } from "@animateicons/react/lucide"

import {
  AnimatedIcon,
  type AnimatedIconHandle,
} from "@/components/ui/animated-icon"

export function Notifications() {
  const iconRef = useRef<AnimatedIconHandle>(null)

  return (
    <button
      aria-label="通知"
      onMouseEnter={() => iconRef.current?.startAnimation()}
      onMouseLeave={() => iconRef.current?.stopAnimation()}
      onFocus={() => iconRef.current?.startAnimation()}
      onBlur={() => iconRef.current?.stopAnimation()}
    >
      <AnimatedIcon ref={iconRef} icon={BellRingIcon} size={24} />
    </button>
  )
}
```

`startAnimation()` 与 `stopAnimation()` 适合按钮、菜单项、保存状态等由父元素控制的场景。组件会保留每个图标自己的动画编排，不会覆盖成统一的动效模板。

<Callout title="减少动态效果">
  AnimateIcons
  会读取用户的减少动态效果偏好。业务上持续循环的状态仍应谨慎使用，避免让无关图标长期运动。
</Callout>

## 图标来源

- [AnimateIcons](https://animateicons.in/icons/lucide)：当前接入的主集合，MIT 许可，通过 npm 按需导入。
- [ItsHover](https://www.itshover.com/icons)：作为专项补充来源，适合寻找 AnimateIcons 尚未覆盖的品牌、技术或动作图标。

新增图标时优先从主集合按需导入；只有主集合缺失并确认许可时，才将单个图标源码收进 registry，避免维护两套重复图标。

## 组件属性

<PropsTable name="animated-icon" />

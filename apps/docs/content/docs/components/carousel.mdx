---
title: 轮播 Carousel
description: 基于原生 CSS 滚动与 scroll-snap 吸附机制构建的轮播组件，支持横向/纵向滑动、循环、自动播放与多列响应式排版。
component: carousel
---

## 基础用法

单卡片横向轮播。可通过前后按钮、触控手势、方向键或底部页码指示器浏览；当前页的圆点会拉伸为胶囊并滑动到目标位置：

<ComponentPreview name="carousel-demo" />

## 安装与引入

通过 CLI 自动添加组件，或手动复制源码至项目中：

<CodeTabs command="@wui-design/cli@latest add @wui/carousel" />

<Steps>

<Step>安装基础依赖与图标库</Step>

```bash
pnpm add motion lucide-react clsx tailwind-merge
```

<Step>复制组件源码到 `components/ui/carousel.tsx`</Step>

<ComponentSource name="carousel" title="components/ui/carousel.tsx" />

</Steps>

## 属性 Props

### Carousel (根容器)

继承原生 `<div>` 元素的全部 HTML 属性：

<PropsTable
  data={[
    {
      prop: "orientation",
      type: '"horizontal" | "vertical"',
      default: '"horizontal"',
      description: "轮播的滚动方向。'horizontal' 为横向滑动；'vertical' 为纵向翻页。",
    },
    {
      prop: "loop",
      type: "boolean",
      default: "false",
      description: "到达边界末项时是否允许无限循环回到起始项。",
    },
    {
      prop: "defaultIndex",
      type: "number",
      default: "0",
      description: "初始化时默认定位展示的项目索引（0-indexed）。",
    },
    {
      prop: "onIndexChange",
      type: "(index: number) => void",
      description: "当前展示的轮播卡片索引发生改变时的回调函数。",
    },
    {
      prop: "autoplay",
      type: "boolean | number",
      default: "false",
      description: "自动播放。传入数字时为每页停留的毫秒数，true 为 5000ms。指针悬停、键盘焦点位于轮播内或页面隐藏时自动暂停；到达末页后回到第一页。",
    },
    {
      prop: "className",
      type: "string",
      description: "应用于外层轮播包裹容器的额外 CSS 类名。",
    },
  ]}
/>

### CarouselContent

承载所有卡片的滚动视口容器：

<PropsTable
  data={[
    {
      prop: "className",
      type: "string",
      description: "应用于滚动视口的额外 CSS 类名（如重置边距或调整固定高度）。",
    },
  ]}
/>

### CarouselItem

单个轮播卡片容器：

<PropsTable
  data={[
    {
      prop: "className",
      type: "string",
      description: "通过 Tailwind 的 basis-* 类名控制一屏显示的卡片数量（例如 basis-full 为单卡片，md:basis-1/2 为一屏两卡，lg:basis-1/3 为一屏三卡）。",
    },
    {
      prop: "data-active",
      type: "attribute",
      description: "项目有一半以上位于视口内时自动添加，可通过 data-[active]: 变体为可见项目设置样式（如非当前项降低不透明度）。",
    },
  ]}
/>

### CarouselPrevious / CarouselNext

翻页控制按钮：

<PropsTable
  data={[
    {
      prop: "className",
      type: "string",
      description: "应用于翻页按钮的额外 CSS 类名（用于覆盖默认定位或图标样式）。",
    },
  ]}
/>

### CarouselDots

页码指示器，每个圆点对应一个吸附位置（一屏多卡时按实际可停靠的页数生成）。当前页圆点拉伸为胶囊并以弹簧动效在圆点间滑动；开启 `autoplay` 时，胶囊内会显示本页的停留进度。少于两页时不渲染。

<PropsTable
  data={[
    {
      prop: "className",
      type: "string",
      description: "应用于指示器容器的额外 CSS 类名。纵向轮播时圆点自动纵向排列。",
    },
  ]}
/>

### useCarousel

在 `Carousel` 内部读取轮播状态，用于构建计数器、同步文案等自定义部件：

<PropsTable
  data={[
    {
      prop: "currentIndex",
      type: "number",
      description: "当前页索引。",
    },
    {
      prop: "count",
      type: "number",
      description: "可停靠的页数。",
    },
    {
      prop: "scrollTo",
      type: "(index: number) => void",
      description: "滚动到指定页。",
    },
    {
      prop: "scrollPrevious / scrollNext",
      type: "() => void",
      description: "切换到上一页 / 下一页，遵循 loop 设置。",
    },
    {
      prop: "canScrollPrevious / canScrollNext",
      type: "boolean",
      description: "当前是否可以继续向前 / 向后切换。",
    },
    {
      prop: "progress",
      type: "MotionValue<number>",
      description: "自动播放时本页的停留进度（0–1），未开启时恒为 0。",
    },
  ]}
/>

## 事件 Events

<PropsTable
  data={[
    {
      prop: "onIndexChange",
      type: "(index: number) => void",
      description: "轮播吸附到新的卡片项目后触发，参数为最新的活动项下标索引。",
    },
    {
      prop: "onScroll",
      type: "(event: React.UIEvent<HTMLDivElement>) => void",
      description: "视口发生原生滚动时触发。",
    },
  ]}
/>

## 使用场景与设计规范

`Carousel` 适用于在有限的视口高度内浏览同质化的系列卡片或媒体：

- **原生性能与手势**：组件基于纯 CSS `scroll-snap-type` 与 `overflow-x: auto`，在触控屏、Mac 触控板双指滑动与鼠标滚轮上具备极致的原生 120fps 顺滑度，不额外劫持手势。
- **一屏多卡展示**：通过给 `CarouselItem` 设置 `className="basis-full sm:basis-1/2 lg:basis-1/3"`，可轻松实现响应式多列轮播布局。
- **重要信息避免隐藏**：核心关键操作或阻断性信息不建议仅放在轮播的第 2 页之后，因为多数用户可能不会主动滑动至末尾。

## 场景示例

### 多卡片响应式轮播 (Multi-card)

结合响应式多卡（`sm:basis-1/2 lg:basis-1/3`）与循环（`loop`），翻页按钮移入标题栏；滑动过程中半露出的卡片通过 `data-[active]:` 变体淡出：

<ComponentPreview name="carousel-cards" />

### 自动播放 (Autoplay)

设置 `autoplay` 后指示器胶囊内显示停留进度，悬停时暂停。配合 `useCarousel` 读取当前页，让左侧文案随图片同步切换：

<ComponentPreview name="carousel-autoplay" />

### 垂直方向轮播 (Vertical)

配置 `orientation="vertical"` 实现纵向滚动的公告栏，结合 `autoplay` 与纵向页码指示器：

<ComponentPreview name="carousel-vertical" />

## 无障碍与交互 Accessibility

- **WAI-ARIA Carousel Pattern**：根容器标注 `role="region"` 与 `aria-roledescription="carousel"`；每个卡片项标注 `role="group"` 与 `aria-roledescription="slide"`。
- **按键与焦点管理**：焦点位于轮播内时，可用 <kbd>←</kbd> / <kbd>→</kbd>（纵向为 <kbd>↑</kbd> / <kbd>↓</kbd>）切换。翻页按钮具备 `aria-label="上一项"` 与 `aria-label="下一项"`，并在到达首末边界且未开启 `loop` 时自动 `disabled`。
- **页码指示器**：每个圆点标注“第 n 页，共 m 页”，当前页带有 `aria-current`。
- **自动播放**：键盘焦点进入或指针悬停时暂停；开启自动播放时视口 `aria-live` 为 `off`，避免读屏软件频繁播报。
- **减少动态效果**：系统开启“减少动态效果”时，翻页改为瞬时跳转，指示器不再做弹簧过渡。

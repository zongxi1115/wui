---
title: 文本形变 Text Morph
description: 在词汇与文本发生变化时，自动追踪共有字符并基于弹性物理模型实现平滑位移与形变过渡。
component: text-morph
---

## 基础用法

最简单的文本形变用法。在按钮状态切换时，新旧文案中的相同字母将直接平滑滑移至新位置，不同字符则渐变融合：

<ComponentPreview name="text-morph-demo" />

## 安装与引入

通过 CLI 自动添加组件，或手动复制源码至项目中：

<CodeTabs command="@wui-design/cli@latest add @wui/text-morph" />

<Steps>

<Step>安装依赖与 Motion 动效库</Step>

```bash
pnpm add motion clsx tailwind-merge
```

<Step>复制组件源码到 `components/ui/text-morph.tsx`</Step>

<ComponentSource name="text-morph" title="components/ui/text-morph.tsx" />

</Steps>

## 属性 Props

`TextMorph` 支持以下配置属性，并继承底层 HTML 元素（默认 `<p>`）的全部属性：

<PropsTable
  data={[
    {
      prop: "children",
      type: "string",
      required: true,
      description: "待进行形变过渡的纯文本字符串。当该值发生变化时自动触发字符追踪动画。",
    },
    {
      prop: "as",
      type: "React.ElementType",
      default: '"p"',
      description: "外层容器所渲染的语义 HTML 标签（如 span, p, h3, div 等）。",
    },
    {
      prop: "className",
      type: "string",
      description: "应用于外层容器元素的额外 CSS 类名。",
    },
  ]}
/>

## 事件 Events

`TextMorph` 会将 `{...props}` 展开到底层元素，因此支持所有原生事件：

<PropsTable
  data={[
    {
      prop: "onClick",
      type: "(event: React.MouseEvent) => void",
      description: "点击文本区域时触发的原生点击事件。",
    },
  ]}
/>

## 使用场景与设计规范

`TextMorph` 专为**微交互中的文字状态转换**而设计，尤其适合操作反馈、分段控制、步骤进度与搜索引导。

- **词汇相关性**：形变效果在“新旧文案共享部分相同字符”时最具视觉魔力（例如 `Save` ➜ `Saving...` ➜ `Saved!`，或 `Download` ➜ `Downloaded`）。如果两段文案完全无相同字符，形变将自然退化为字符淡入淡出。
- **避免过长段落形变**：仅将 `TextMorph` 用于短句、单词或标题（建议小于 40 个字符）。对几百字的文章正文执行字符级全局追踪会消耗不必要的布局计算。
- **与按钮/徽章深度融合**：在提交按钮中使用 `TextMorph`，可以避免按钮在加载和成功状态之间发生突兀的白屏跳闪，提供原生应用级的精致触感。

## 场景示例

### 状态操作按钮

结合表单提交与异步请求状态（如 Idle、Loading、Success），实现无跳闪的丝滑按钮状态反馈：

<ComponentPreview name="text-morph-demo" />

### 分段筛选标签栏

在看板过滤栏或数据视图切换中，保持当前选中项标题与数量统计的动态平滑过渡：

<ComponentPreview name="text-morph-tabs" />

### 智能搜索提示轮播

在 AI 提问框或全局检索框中，优雅轮换引导提示文本：

<ComponentPreview name="text-morph-search-bar" />

### CI/CD 部署流水线状态

展示长耗时任务在不同阶段之间的递进演进：

<ComponentPreview name="text-morph-stepper" />

## 无障碍与交互 Accessibility

- **屏幕阅读器支持**：外层容器绑定 `aria-label={children}`，内部各个独立的形变 `motion.span` 标记有 `aria-hidden="true"`，保证辅助技术始终将文本作为完整词语朗读，不会因单个字母的位移而产生碎片化发音。
- **系统减少动态偏好（Reduced Motion）**：当用户系统启用了“减少动态效果（prefers-reduced-motion）”时，组件将跳过 `LayoutGroup` 空间位移动画，直接以无延迟的静态文本更新。
- **空格与排版保护**：空格字符内部自动映射为非换行空格 `\u00a0`，防止因为 HTML 空格折叠特性导致词间距在形变计算时发生塌陷。

---
title: 表格 Table
description: 保留原生语义与无障碍结构的响应式表格组件，支持紧凑密度、斑马纹、冻结表头与左右固定列。
component: table
---

## 基础用法

最基础的表格用法。使用语义化的 `Table`、`TableHeader`、`TableBody`、`TableRow`、`TableHead` 和 `TableCell` 组织结构化数据：

<ComponentPreview name="table-demo" />

## 安装与引入

通过 CLI 自动添加组件，或手动复制源码至项目中：

<CodeTabs command="@wui-design/cli@latest add @wui/table" />

<Steps>

<Step>安装基础依赖与工具函数</Step>

```bash
pnpm add class-variance-authority clsx tailwind-merge
```

<Step>复制组件源码到 `components/ui/table.tsx`</Step>

<ComponentSource name="table" title="components/ui/table.tsx" />

</Steps>

## 属性 Props

### Table

`Table` 最外层封装了支持横向滚动的容器元素，并接受原生 `<table>` 的全部 HTML 属性：

<PropsTable
  data={[
    {
      prop: "density",
      type: '"default" | "compact"',
      default: '"default"',
      description: "控制表格单元格内边距与行高的视觉密度。",
    },
    {
      prop: "striped",
      type: "boolean",
      default: "false",
      description: "是否为偶数数据行增加斑马纹浅灰背景，提升多行连续阅读体验。",
    },
    {
      prop: "stickyHeader",
      type: "boolean",
      default: "false",
      description: "在容器发生纵向滚动时，表头是否固定在顶部吸顶可见。",
    },
    {
      prop: "containerClassName",
      type: "string",
      description: "应用于外层溢出滚动容器（div[data-slot=table-container]）的额外 CSS 类名。",
    },
    {
      prop: "className",
      type: "string",
      description: "应用于底层 <table> 元素的额外 CSS 类名。",
    },
  ]}
/>

### TableHead

表头单元格，接受原生 `<th>` 的全部 HTML 属性：

<PropsTable
  data={[
    {
      prop: "pinned",
      type: '"left" | "right"',
      description: "将列冻结在横向滚动容器的左侧或右侧边缘。",
    },
    {
      prop: "pinOffset",
      type: "number | string",
      default: "0",
      description: "距离固定边缘的偏移量（像素或 CSS 长度），连续固定多列时用于计算层叠位置。",
    },
    {
      prop: "className",
      type: "string",
      description: "应用于表头单元格的额外 CSS 类名。",
    },
  ]}
/>

### TableCell

数据单元格，接受原生 `<td>` 的全部 HTML 属性：

<PropsTable
  data={[
    {
      prop: "pinned",
      type: '"left" | "right"',
      description: "将单元格固定在横向滚动容器的左侧或右侧边缘，需与同列的 TableHead 保持一致。",
    },
    {
      prop: "pinOffset",
      type: "number | string",
      default: "0",
      description: "距离固定边缘的偏移量，需与同列的 TableHead 保持一致。",
    },
    {
      prop: "className",
      type: "string",
      description: "应用于数据单元格的额外 CSS 类名。",
    },
  ]}
/>

### 结构子组件

`TableHeader`（`<thead>`）、`TableBody`（`<tbody>`）、`TableFooter`（`<tfoot>`）、`TableRow`（`<tr>`）和 `TableCaption`（`<caption>`）均透传对应原生 HTML 元素的全部属性与引用。

## 事件 Events

所有表格组件均完整透传原生 DOM 事件，常用事件如下：

<PropsTable
  data={[
    {
      prop: "onClick",
      type: "(event: React.MouseEvent<HTMLTableRowElement>) => void",
      description: "在 TableRow 或 TableCell 上点击时触发，常用于行点击查看详情或触发行选择。",
    },
    {
      prop: "onPointerEnter",
      type: "(event: React.PointerEvent<HTMLTableRowElement>) => void",
      description: "指针移入数据行时触发，可用于显示悬浮操作按钮或关联图表高亮。",
    },
    {
      prop: "onPointerLeave",
      type: "(event: React.PointerEvent<HTMLTableRowElement>) => void",
      description: "指针离开数据行时触发。",
    },
    {
      prop: "onKeyDown",
      type: "(event: React.KeyboardEvent<HTMLTableRowElement>) => void",
      description: "在可聚焦的数据行或单元格上按下键盘按键时触发，用于支持键盘快捷操作。",
    },
  ]}
/>

## 使用场景与设计规范

`Table` 适用于呈现列结构清晰、需要进行纵向扫描与横向对比的二维结构化数据。

- **表格 vs 描述列表 Descriptions**：若数据为同构的多条业务记录（如 50 笔订单明细、团队成员列表），应使用 `Table`；若为单个实体的多维度详情画像（如单个订单的详细信息），应使用 `Descriptions`。
- **数据对齐原则**：
  - **文本列**：左对齐（默认）。
  - **数值与金额列**：右对齐，并添加 `tabular-nums` 字体特性类名，保证数字与小数点垂直精确对齐，方便视觉扫视比对。
  - **状态与标签列**：居中或左对齐，宽度相对固定。
  - **操作列**：右对齐，建议固定在表格最右侧（`pinned="right"`）。
- **固定列（Sticky Pinning）规范**：当表格列数较多且存在横向滚动时，建议将前 1~2 列关键标识（如复选框、名称、ID）固定在左侧，将操作列固定在右侧，确保用户在滚动浏览宽数据时不迷失当前上下文。

## 场景示例

### 紧凑密度与斑马纹

在运维监控、接口日志等高密度数据分析场景下，使用 `density="compact"` 降低内边距，配合 `striped` 斑马纹与 `TableFooter` 汇总行：

<ComponentPreview name="table-compact" />

### 行选择与批量操作

在数据管理列表中，通过 `TableRow` 的 `data-state="selected"` 属性呈现选中背景高亮，并配合工具栏提供批量处理操作：

<ComponentPreview name="table-selection" />

### 复杂数据表格（固定列 / 树形 / 排序 / 筛选 / 拖拽）

针对企业级复杂中后台场景，`Table` 可以无缝承载级联多级表头、树形展开折叠、同级拖动排序、表头多维筛选与左右双向冻结列：

<ComponentPreview name="table-advanced" />

## 无障碍与交互 Accessibility

- **语义化结构**：组件输出规范的 `<table>`、`<thead>`、`<tbody>`、`<tfoot>`、`<tr>`、`<th>` 和 `<td>` 原生元素，读屏器能够自动识别行列数与单元格坐标。
- **表头关联**：`TableHead` 自动渲染为 `<th>` 并具有 `align-middle` 与 `font-medium` 样式，在复杂多级表头下可通过 `colSpan` 与 `rowSpan` 维持无障碍关系树。
- **键盘焦点与选中**：当表格支持行选择时，使用带有明确 `aria-label` 的 `Checkbox` 组件；当整行支持点击时，建议通过 `tabIndex={0}` 与 `onKeyDown` 提供回车键响应支持。
- **横向滚动容器**：外层容器具有 `overflow-auto`，在触控与键盘 Tab 导航中可顺畅聚焦与滚动，避免内容在小屏幕下溢出截断。

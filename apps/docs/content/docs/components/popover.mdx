---
title: 气泡浮层 Popover
description: 在指定触发锚点周围展示富文本内容、轻量表单或操作卡片，支持自动碰撞避让与完整的焦点管理。
component: popover
---

## 基础用法

点击触发按钮即可在就近位置弹出一个浮层卡片：

<ComponentPreview name="popover-demo" />

## 安装与引入

通过 CLI 自动添加组件，或手动复制源码至项目中：

<CodeTabs command="@wui-design/cli@latest add @wui/popover" />

<Steps>

<Step>安装基础依赖与图标库</Step>

```bash
pnpm add radix-ui lucide-react clsx tailwind-merge
```

<Step>复制组件源码到 `components/ui/popover.tsx`</Step>

<ComponentSource name="popover" title="components/ui/popover.tsx" />

</Steps>

## 属性 Props

### Popover (根组件)

<PropsTable
  data={[
    {
      prop: "open",
      type: "boolean",
      description: "受控模式下的浮层可见状态。",
    },
    {
      prop: "defaultOpen",
      type: "boolean",
      default: "false",
      description: "非受控模式下的初始打开状态。",
    },
    {
      prop: "onOpenChange",
      type: "(open: boolean) => void",
      description: "浮层打开或关闭时触发的回调函数。",
    },
    {
      prop: "modal",
      type: "boolean",
      default: "false",
      description: "是否将 Popover 渲染为模态（阻止页面其余部分的交互与滚动）。",
    },
  ]}
/>

### PopoverContent

<PropsTable
  data={[
    {
      prop: "side",
      type: '"top" | "right" | "bottom" | "left"',
      default: '"bottom"',
      description: "浮层优先出现在触发锚点的哪一侧。",
    },
    {
      prop: "align",
      type: '"start" | "center" | "end"',
      default: '"center"',
      description: "浮层与触发锚点在交叉轴上的对齐方式。",
    },
    {
      prop: "sideOffset",
      type: "number",
      default: "6",
      description: "浮层与触发锚点之间的间距（单位：像素）。",
    },
    {
      prop: "showArrow",
      type: "boolean",
      default: "false",
      description: "是否渲染指向触发锚点的小三角箭头指示器。",
    },
    {
      prop: "className",
      type: "string",
      description: "应用于浮层容器的额外 CSS 类名。",
    },
  ]}
/>

### PopoverTrigger / PopoverClose

<PropsTable
  data={[
    {
      prop: "asChild",
      type: "boolean",
      default: "false",
      description: "启用后将打开/关闭行为绑定到唯一子元素（如 `<Button>`）。",
    },
  ]}
/>

## 事件 Events

<PropsTable
  data={[
    {
      prop: "onOpenChange",
      type: "(open: boolean) => void",
      description: "浮层可见性发生变化时调用（点击外部、按下 Esc 键或点击关闭按钮）。",
    },
    {
      prop: "onInteractOutside",
      type: "(event: CustomEvent) => void",
      description: "用户在浮层外部发生点击或聚焦时触发，可通过 event.preventDefault() 阻止自动关闭。",
    },
    {
      prop: "onEscapeKeyDown",
      type: "(event: KeyboardEvent) => void",
      description: "用户按下键盘 Esc 键时触发。",
    },
  ]}
/>

## 使用场景与设计规范

`Popover` 用于就地承载比 Tooltip 更加复杂的可交互内容：

- **选型对比**：
  - **Popover vs Tooltip**：Tooltip 仅用于鼠标悬停展示纯文本提示，禁止包含可点击链接或输入框；Popover 专为包含表单、多选框、按钮等交互设计。
  - **Popover vs Dialog**：Popover 紧贴触发元素，适合轻量设置与单列筛选；Dialog 居中遮罩，适合大表单、重要确认与复杂流程。
- **避免过重表单**：Popover 内部宜保持紧凑（推荐 1~3 个字段），如果需要滚动长表单，请改用 Sheet 侧抽屉或 Dialog 模态弹窗。
- **语义化标题配对**：推荐始终在内部放置 `PopoverTitle` 与 `PopoverDescription`，为屏幕阅读器建立清晰的无障碍无缝解读。

## 场景示例

### 快捷筛选与多选条件面板

在表格工具栏中展开包含状态多选与重置操作的筛选气泡：

<ComponentPreview name="popover-filter" />

### 轻量配置与表单输入

受控模式下调整画布宽高参数，支持取消与保存：

<ComponentPreview name="popover-form" />

## 无障碍与交互 Accessibility

- **焦点管理**：浮层打开后，焦点会自动安全转移到浮层内的第一个可交互元素；关闭后焦点精确恢复至触发按钮。
- **快捷键**：按下 <kbd>Esc</kbd> 即可随时安全退出并关闭浮层。
- **视口碰撞翻转**：内置自动边界检测（Floating UI / Radix Popper），当屏幕下方空间不足时，自动翻转至上方或侧面，防止内容被浏览器视口截断。

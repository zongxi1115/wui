---
title: 渐进模糊 Progressive Blur
description: 利用多层重叠梯度遮罩与 Backdrop Filter 实现边缘平滑衰减虚化、防止硬截断的视觉层次组件。
component: progressive-blur
---

## 基础用法

在动态列表或长文本卡片底部叠加多层渐进模糊，使超出部分自然虚化并提供滚动心理暗示：

<ComponentPreview name="progressive-blur-demo" />

## 安装与引入

通过 CLI 自动添加组件，或手动复制源码至项目中：

<CodeTabs command="@wui-design/cli@latest add @wui/progressive-blur" />

<Steps>

<Step>安装基础依赖与动效库</Step>

```bash
pnpm add motion class-variance-authority clsx tailwind-merge
```

<Step>复制组件源码到 `components/ui/progressive-blur.tsx`</Step>

<ComponentSource name="progressive-blur" title="components/ui/progressive-blur.tsx" />

</Steps>

## 属性 Props

<PropsTable
  data={[
    {
      prop: "direction",
      type: '"top" | "right" | "bottom" | "left"',
      default: '"bottom"',
      description: "虚化衰减的主边缘方向（即模糊度最高的一侧）。",
    },
    {
      prop: "blurLayers",
      type: "number",
      default: "8",
      description: "用于平滑过度的遮罩层分段数量（最小自动限制为 2）。",
    },
    {
      prop: "blurIntensity",
      type: "number",
      default: "0.25",
      description: "每个分层逐级递增的模糊增量系数（像素）。数值越大，模糊总强度越高。",
    },
    {
      prop: "className",
      type: "string",
      description: "应用于外层定位容器的 CSS 类名（通常配置 absolute inset-x-0 bottom-0 等定位）。",
    },
  ]}
/>

## 事件 Events

该组件为纯展示型分层视觉遮罩层，自身不派发专用业务交互事件，默认标记 `pointer-events: none` 且不阻挡底层鼠标点击。

## 使用场景与设计规范

`ProgressiveBlur` 适用于**截断列表提示、吸顶导航栏底部、图片渐隐遮罩**：

- **告别生硬的纯色遮罩**：传统的 `bg-gradient-to-t from-background` 容易遮盖住文字的色彩，而渐进模糊（Backdrop Blur）则能保留底层色相同时让文本轮廓柔和化。
- **层数建议**：通常 6 ~ 8 层可以在极佳的平滑度与 GPU 显存消耗之间取得完美平衡。

## 场景示例

### 上下方多方向虚化对比

展示顶部与底部模糊在不同容器中的应用：

<ComponentPreview name="progressive-blur-directions" />

## 无障碍与交互 Accessibility

- **点击穿透**：容器默认携带 `pointer-events-none`，用户隔着模糊区域点击下方的链接或按钮依然能够精准响应。
- **无障碍树隐藏**：默认设置 `aria-hidden="true"`，屏幕阅读器不受任何视觉滤镜干扰。
